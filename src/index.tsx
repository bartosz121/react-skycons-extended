import React, { useEffect, useRef } from 'react';

//@ts-ignore
import { Skycons } from "./skycons/skycons"

export interface Props {} // TODO


export const Thing: React.FC<Props> = ({}) => {
  return(
    <div></div>
  )
};
